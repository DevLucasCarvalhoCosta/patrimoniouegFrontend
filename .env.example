# ===========================================
# UEG Patrimonial - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env, .env.development ou .env.production
# e configure os valores conforme seu ambiente.

# Base da API (relativa ou absoluta)
# Em desenvolvimento, use /api e configure o proxy no vite.config.ts
# Em produção, use /api se tiver proxy reverso configurado
VITE_API_BASE=/api

# URL do backend para desenvolvimento (usado pelo proxy do Vite)
# VITE_DEV_API_TARGET=https://seu-servidor-api.com

# Base para o portal CKAN (Dados Abertos)
VITE_CKAN_BASE=/ckan

# URL base para assets (imagens) - deixe vazio para usar o mesmo domínio
# VITE_ASSETS_BASE_URL=
